name: version-watcher-bot

services:
  server:
    container_name: version-watcher-bot
    build: 
      context: .
      dockerfile: Dockerfile
    mem_limit: 1G
    restart: on-failure:3
    ports:
      - 3030:3030
    env_file:
      - path: "./.env"
        required: true      
    